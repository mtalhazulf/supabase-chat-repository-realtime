node_modules
.next
tsconfig.tsbuildinfo
out
build
dist
# `.env` is intentionally NOT ignored for this demo (see disclaimer in the file).
# `.env.local` IS ignored so you can override demo values with secrets locally.
.env*.local
.vercel
*.tsbuildinfo
next-env.d.ts
.DS_Store
bun.lock
bun.lockb
